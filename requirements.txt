# Cœur Django/DRF — absents de ce fichier jusqu'ici alors qu'installés
# manuellement dans le venv (lacune pré-existante découverte en P1.6 : un
# `pip install -r requirements.txt` sur une machine propre ou en CI
# n'installait ni Django ni DRF). Versions figées = celles réellement en
# service dans le venv de développement.
Django==5.2.4
djangorestframework==3.16.0
django-cors-headers==4.9.0
requests==2.34.2
django-environ==0.14.0
psycopg2-binary==2.9.12
python-dateutil==2.9.0.post0
Pillow==11.1.0

# P10.3 — envoi des notifications push (Firebase Cloud Messaging)
firebase-admin==6.5.0

# P1.6 — documentation API + tests
drf-spectacular==0.28.0
pytest==8.3.4
pytest-django==4.9.0
pytest-cov==6.0.0
ruff==0.8.6
black==24.10.0

# Migration hébergement VPS+Coolify — serveur ASGI (HTTP + WebSocket dans un
# seul process, remplace le WSGI PythonAnywhere), Channels (déjà scaffoldé
# dans yeki/consumers.py + config/asgi.py, jamais installable jusqu'ici),
# channel layer Redis (nécessaire dès qu'il y a plus d'un process — le layer
# mémoire par défaut de Channels ne fonctionne qu'en single-process), et
# WhiteNoise pour servir les fichiers statiques directement (remplace le
# mapping de fichiers statiques du tableau de bord PythonAnywhere, qui
# n'existe plus sur ce nouvel hébergement).
daphne==4.1.2
channels==4.2.0
channels-redis==4.2.1
whitenoise==6.8.2
